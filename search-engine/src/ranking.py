"""
Ranking module: scores documents using TF-IDF.
"""


def rank(index: dict, terms: list[str], matching_urls: list[str]) -> list[tuple[str, float]]:
    """Return (url, score) pairs sorted by TF-IDF score descending."""
    pass
